version: 2

models:
  - name: bronze_listings
    config:
      materialized: incremental
  - name: bronze_bookings
    config:
      materialized: incremental
  - name: bronze_hosts
    config:
      materialized: incremental
